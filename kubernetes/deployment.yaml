apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: admission-webhooks
  name: admission-webhooks
  namespace: admission-webhooks
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admission-webhooks
  template:
    metadata:
      labels:
        app: admission-webhooks
    spec:
      containers:
      - image: registry.harrisonkiang.com/container-registry/admission-webhooks:fd9807a954ca2491d803d885cefcea0ac0ee903b
        name: admission-webhooks
        ports:
        - containerPort: 8080
          protocol: TCP
          name: http
        env:
        - name: SSL_KEYFILE
          value: /home/ssl/tls.key
        - name: SSL_CERTFILE
          value: /home/ssl/tls.crt
        volumeMounts:
          - name: ssl
            mountPath: "/home/ssl"
            readOnly: true
      volumes:
        - name: ssl
          secret:
            secretName: harrisonkiang-dot-com-wildcard-tls
