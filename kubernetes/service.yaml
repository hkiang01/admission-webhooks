apiVersion: v1
kind: Service
metadata:
  labels:
    app: admission-webhooks
  name: admission-webhooks
  namespace: admission-webhooks
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: admission-webhooks
