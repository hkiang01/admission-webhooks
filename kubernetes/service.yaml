apiVersion: v1
kind: Service
metadata:
  labels:
    app: admission-webhooks
  name: admission-webhooks
  namespace: admission-webhooks
spec:
  ports:
  - port: 443
    protocol: TCP
    targetPort: https
  selector:
    app: admission-webhooks
